# Required: Anthropic API key
ANTHROPIC_API_KEY=sk-ant-api03-...

# Server
PORT=3001
FRONTEND_URL=http://localhost:5173

# Model for agents and graders
MODEL_ID=claude-sonnet-4-20250514

# Max grading loop iterations per stage (assumption, steelman, synthesis)
MAX_LOOP_ITERATIONS=4
# Alias also supported: MAX_ITERATIONS

# In-memory session expiry (minutes); cleanup runs every 5 minutes
SESSION_TTL_MINUTES=30
