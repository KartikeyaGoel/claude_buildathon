{
  "name": "claude-buildathon",
  "private": true,
  "workspaces": [
    "crucible/shared",
    "crucible/backend",
    "crucible/frontend"
  ],
  "scripts": {
    "build": "npm run build -w @crucible/shared && npm run build -w crucible-backend",
    "test": "npm run test -w crucible-backend",
    "ci": "npm run build && npm run test && npm run security:sql -w crucible-backend"
  }
}
